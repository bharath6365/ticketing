apiVersion: skaffold/v2alpha3
kind: Config
deploy:
  kubectl:
    manifests:
      - ./infra/k8s/*
build:
  local:
    push: false
  artifacts:
    - image: bharath6365/auth
      context: auth
      docker:
        dockerfile: Dockerfile
      sync:
        manual:
          - src: 'src/**/*.ts'
            dest: .
    - image: bharath6365/client
      context: client
      docker:
        dockerfile: Dockerfile
      sync:
        manual:
        # No Src Folder
          - src: '**/*.js'
            dest: .
